package pl.patkkka.tictactoe.config;

import java.util.Collections;
import java.util.HashSet;
import java.util.Set;
import java.util.stream.IntStream;

/**
 * Created by patrycja on 29.06.17.
 */
public class BoardCharacters {
//    public static Set<String> chars = new HashSet<String>(PlayerCharacters.chars);
//    public static final String EMPTY_FIELD_CHARACTER = "-";
//
//    public BoardCharacters() {
//        chars.add(EMPTY_FIELD_CHARACTER);
//        Integer[] ints = IntStream.range(0, Configuration.BOARD_FIELDS_NO).boxed().toArray(Integer[]::new);
//        String[] strings = new String[ints.length];
//        for (int i=0; i<strings.length; i++){
//            strings[i] = ints[i].toString();
//        }
//        Collections.addAll(chars, strings);
//    }
}
